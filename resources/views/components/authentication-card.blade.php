<div class="w-full max-w-md">
    <div class="flex justify-center mb-8 press">
        {{ $logo }}
    </div>

    <div class="rounded-2xl border p-8 sm:p-10" style="background: var(--paper-deep); border-color: var(--line); box-shadow: 0 1px 3px rgba(34, 27, 18, 0.05);">
        {{ $slot }}
    </div>
</div>
