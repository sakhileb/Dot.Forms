<IfModule mod_rewrite.c>
    RewriteEngine On

    # If document root points to project root, rewrite all requests to /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
